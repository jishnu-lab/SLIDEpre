#' Calculate the mean element-wise absolute deviation between two matrices.
#'
#' @param A a matrix
#' @param B a matrix of same dimensions as \code{A}
#' @return mean absolute deviation

Adev <- function(mat_A, mat_B) {
  return (mean(abs(A - B)))
}
